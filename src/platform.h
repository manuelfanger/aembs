#ifndef PLATFORM_H__
#define PLATFORM_H__

#define PL_CONFIG_USE_OLED_LCD (1)
#define PL_CONFIG_USE_INTEGRATION (1)
#define PL_CONFIG_USE_GAME_EVENTS (1)
#define PL_CONFIG_USE_CREATE_GAME_LEVEL (1)
#define PL_CONFIG_USE_OLED_LCD (1)
#define PL_CONFIG_USE_OLED_LCD (1)
#define PL_CONFIG_USE_OLED_LCD (1)

void PL_Init(void);

void PL_Deinit(void);

#endif